# Secrets file. Lives in the same place as the config file and is used to supply secrets to the config.
# These are only needed if the functionalities are used. 
api:
  # The signing key for the jwt token in the api
  signing_key: "Throughout heaven and earth I am the honored one"

daemon:
  # Credentials for the smtp-server
  notification:
    email_user: "username"
    email_pass: "password"
